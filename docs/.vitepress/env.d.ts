// Ambient declarations for VitePress's TypeScript-aware editors.
// CSS is imported for its side effects only (Vite handles it at build time);
// this stops TS from complaining about the missing module declaration.
declare module '*.css';
